<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartattendance">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentMain"></activity>
        <activity android:name=".ViewQRCode" />
        <activity
            android:name=".AdminMain"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Student.StudentMain"
            android:theme="@style/AppTheme.TransparentActionBar" />
        <activity android:name=".Authentication.StudentRegistration" />
        <activity android:name=".Authentication.StudentLogin" />
        <activity android:name=".Authentication.AdminLogin" />
        <activity android:name=".PreLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>